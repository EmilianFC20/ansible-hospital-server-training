[defaults]
# This lab uses the shared project inventory.
# Run commands from the project root so the top-level ansible.cfg takes effect,
# or override with: ansible all -i ../inventory/hosts.ini -m ping
inventory       = ../inventory/hosts.ini
host_key_checking = False
