[frontend]
ubuntu-01 ansible_host=10.73.73.175

[backend]
ubuntu-02 ansible_host=10.73.73.202

[hospital:children]
frontend
backend

# NOTE: Since you are using NAT, ensure you can ping these IPs
# from your host machine. If you have trouble, consider
# switching to "Bridged" networking in VMWare settings.
#
# Connection variables (ansible_user, ansible_python_interpreter) are
# defined in the shared group_vars/ directory — see:
#   inventory/group_vars/frontend.yml   ← ubuntu-01 connection settings
#   inventory/group_vars/backend.yml    ← ubuntu-02 connection settings
#
# Do NOT store passwords here. Use SSH keys (ssh-copy-id) or encrypt
# with ansible-vault:
#   ansible-vault encrypt_string 'MyPassword' --name 'ansible_ssh_pass'
